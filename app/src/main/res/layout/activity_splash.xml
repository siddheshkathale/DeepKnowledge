<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".view.Activities.SplashActivity">

        <ImageView

            android:id="@+id/imgSplash"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:scaleType="centerCrop"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@mipmap/splashbg" />

        <ImageView
            android:id="@+id/imgLady"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:scaleType="centerCrop"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="@+id/imgSplash"
            app:srcCompat="@mipmap/lady" />

        <ImageView
            android:id="@+id/imgGrass"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:scaleType="centerCrop"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="@+id/imgSplash"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="@+id/imgSplash"
            app:layout_constraintTop_toTopOf="@+id/imgSplash"
            app:srcCompat="@mipmap/grass" />

        <ImageView
            android:id="@+id/imgSplashBg"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:scaleType="centerCrop"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="@+id/imgSplash"
            app:layout_constraintEnd_toEndOf="@+id/imgSplash"
            app:layout_constraintStart_toStartOf="@+id/imgSplash"
            app:layout_constraintTop_toTopOf="@+id/imgSplash"
            app:srcCompat="@mipmap/splash_screen_bg" />
    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>

